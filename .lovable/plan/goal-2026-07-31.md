## Goal

Extend the homepage into a single scrolling page: Hero → About the Studio → Games & Apps, matching the reference screenshots, with the existing navbar links scrolling to each section.

## 1. Ticker strip (under navbar, About section)

A thin bordered marquee band with dot-separated items: GAME DEVELOPMENT · UNITY & UNREAL ENGINE · MOBILE APPS · IOS & ANDROID · UI/UX DESIGN · INDIE SPIRIT · MADE IN INDIA. Slow horizontal loop, teal dots, pauses on hover.

## 2. About the Studio

Two-column split (stacks on mobile):

- Left: an orbital diagram — concentric wireframe rings around the crest logo + "NEXTRIBE STUDIOS" lockup, with small floating chips ("2025 FOUNDED", "HYD INDIA") and glowing nodes riding the orbits. Built with SVG + Motion, reusing the existing brand palette and glow utilities.
- Right: "— ABOUT THE STUDIO" eyebrow, headline "Small Team. / Big Worlds." (white + cyan), intro paragraph, and three icon rows: Craft-First Development, Indie Spirit Pro Polish, Player-Centric Always — each with a rounded icon tile and short description.

## 3. Games & Apps section

- Heading "GAMES & APPS WE'RE PROUD OF." with "PROUD OF" in orange, plus intro paragraph.
- A 2-column card grid (1 column on mobile) of four titles: Word Cross, Planked!, Tribal Tycoon, Circuit Crusher.
- Each card: cover art area with a status badge (Launched Title / Puzzle Platformer / In-Development / Launching Soon), category line, title, description, platform icons, and tag pills. Cards lift and glow on hover.
- Card artwork: I'll generate stylised cover images per game to match each description, since the originals aren't available. Say the word if you'd rather upload the real screenshots and I'll wire those in instead.

## 4. Page wiring

- `src/routes/index.tsx` renders Hero, About, Games in order; the existing `sr-only` placeholder sections are replaced by the real ones with matching ids.
- Navbar links smooth-scroll to `#about`, `#services`, `#work`; navbar becomes sticky with a subtle blur backdrop once scrolled past the hero.
- "Scroll to explore" cue at the bottom of each full-height section.
- Ambient background continues behind all sections; particle density trimmed lower down so content stays readable.
- Sections fade/slide in on scroll via Motion `whileInView`, disabled under reduced motion.

## Technical notes

New files: `src/components/site/TickerStrip.tsx`, `About.tsx`, `AboutOrbit.tsx`, `Games.tsx`, `GameCard.tsx`, plus `src/data/games.ts`. Edited: `src/routes/index.tsx`, `src/components/site/Navbar.tsx`, `src/styles.css` (marquee + orbit keyframes). All colors stay on existing tokens. No backend changes.
